# Single source of truth for app version.
# Bump VERSION_CODE on every release (must always increase).
# CI reads this file, tags the commit, and publishes a GitHub Release automatically.
VERSION_NAME=1.0.0
VERSION_CODE=1
